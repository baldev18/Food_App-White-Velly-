

# 🧠 1. Project Overview

**Project Name:** Cafe Management App
**Platform:** Flutter (Android / iOS)
**Type:** Offline-first mobile application

### 🎯 Objective

Build a cafe app where:

* Customers can browse menu & order
* Admin can manage items & orders
* All data stored locally (no server)

---

# 🧱 2. Tech Stack

* **Framework:** Flutter
* **Language:** Dart
* **Database:** SQLite or Hive
* **State Management:** Provider / Riverpod

---

# 📱 3. App Features

## 👤 Customer Features

* View menu
* Search items
* Add to cart
* Place order
* View order history

## 🛠️ Admin Features

* Add/Edit/Delete menu
* Manage categories
* View all orders
* Update order status

---

# 🏗️ 4. App Architecture

Use **Clean Architecture + MVVM**

```
lib/
│── models/
│── views/
│── viewmodels/
│── services/
│── database/
│── utils/
```

---

# 🗄️ 5. Database Design (SQLite)

## 🧑 Users Table

```sql
id INTEGER PRIMARY KEY
name TEXT
email TEXT
password TEXT
role TEXT
```

---

## 📂 Categories Table

```sql
id INTEGER PRIMARY KEY
name TEXT
image TEXT
```

---

## 🍔 Menu Table

```sql
id INTEGER PRIMARY KEY
name TEXT
description TEXT
price REAL
category_id INTEGER
image TEXT
```

---

## 🛒 Cart Table

```sql
id INTEGER PRIMARY KEY
user_id INTEGER
```

---

## 🧾 Cart Items Table

```sql
id INTEGER PRIMARY KEY
cart_id INTEGER
menu_id INTEGER
quantity INTEGER
```

---

## 📦 Orders Table

```sql
id INTEGER PRIMARY KEY
user_id INTEGER
total_price REAL
status TEXT
created_at TEXT
```

---

## 📋 Order Items Table

```sql
id INTEGER PRIMARY KEY
order_id INTEGER
menu_id INTEGER
quantity INTEGER
price REAL
```

---

# 🔄 6. App Flow

### 🔐 Authentication Flow

1. User registers
2. Data saved in SQLite
3. Login checks local DB

---

### 🍽️ Ordering Flow

1. User selects items
2. Adds to cart
3. Places order
4. Order saved locally
5. Admin updates status

---

# 📊 7. Screens List

## 👤 Customer Screens

* Splash Screen
* Login/Register
* Home (Menu)
* Item Details
* Cart
* Checkout
* Order History

---

## 🛠️ Admin Screens

* Dashboard
* Manage Menu
* Add/Edit Item
* Orders List
* Order Details

---

# 🔌 8. Services Layer (NO APIs)

Instead of APIs, you’ll create **local services**:

## Example: Menu Service

```dart
class MenuService {
  Future<List<Menu>> getMenu() async {
    return await db.query('menu');
  }

  Future<void> addItem(Menu item) async {
    await db.insert('menu', item.toMap());
  }
}
```

---

## Example: Order Service

```dart
class OrderService {
  Future<void> placeOrder(Order order) async {
    await db.insert('orders', order.toMap());
  }

  Future<List<Order>> getOrders() async {
    return await db.query('orders');
  }
}
```

---

# 🧠 9. State Management

Use **Provider**:

```dart
class CartProvider with ChangeNotifier {
  List<CartItem> items = [];

  void addItem(CartItem item) {
    items.add(item);
    notifyListeners();
  }
}
```

---

# 🎨 10. UI Design Guidelines

* Use Material UI
* Colors:

  * Primary: Brown ☕
  * Accent: Cream
* Use Cards for menu items
* Bottom navigation for main screens

---

# 🔐 11. Security (Basic)

* Store passwords hashed (optional)
* Use local session (SharedPreferences)

---

# 📦 12. Packages Needed

Add to `pubspec.yaml`:

```yaml
dependencies:
  flutter:
  sqflite:
  path_provider:
  provider:
  shared_preferences:
  intl:
```

---

# 📈 13. Future Enhancements

* Convert to online system (add APIs later)
* Payment integration
* Push notifications
* Firebase sync

---

# 📊 14. Project Summary

| Component | Count |
| --------- | ----- |
| Screens   | 8–10  |
| Tables    | 7     |
| Services  | 4–6   |
| Models    | 6–8   |

---

# ✅ Final Notes

* No APIs needed (everything local)
* Works offline
* Easy to convert to online later
* Perfect for college/project demo

---

If you want next, I can:
✅ Generate **complete Flutter code (UI + DB)**
✅ Give **step-by-step build guide**
✅ Create **ready-made project folder**

Just tell me 👍


-------------------------------------------------------------------------------------------------------------------

🔌 📋 COMPLETE API LIST (For Cafe App)

👉 Total APIs: 16 APIs

🔐 1. Authentication APIs (4)

Register User

POST /auth/register

Login

POST /auth/login

Logout

POST /auth/logout

Get Profile

GET /auth/profile
🍔 2. Menu APIs (5)

Get All Categories

GET /categories

Add Category (Admin)

POST /categories

Get Menu Items

GET /menu

Add Menu Item (Admin)

POST /menu

Update/Delete Menu Item

PUT /menu/{id}
DELETE /menu/{id}
🛒 3. Cart APIs (3)

Add to Cart

POST /cart/add

Get Cart

GET /cart

Remove Item

DELETE /cart/{id}
📦 4. Order APIs (4)

Place Order

POST /orders

Get User Orders

GET /orders

Get All Orders (Admin)

GET /orders/all

Update Order Status

PUT /orders/{id}/status

-------------------------------------------------------------------------------------------------------------------

👤 1. Users Table
Column Name	Type	Description
id	INTEGER (PK)	Unique user ID
name	TEXT	User name
email	TEXT	User email
password	TEXT	User password
role	TEXT	admin or customer


📂 2. Categories Table

Column Name	Type	Description
id	INTEGER (PK)	Category ID
name	TEXT	Category name (Coffee, Tea)
image	TEXT	Image path

🍔 3. Menu Table


Column Name	Type	Description
id	INTEGER (PK)	Item ID
name	TEXT	Item name
description	TEXT	Item details
price	REAL	Price
category_id	INTEGER (FK)	लिंक to categories
image	TEXT	Image path

🛒 4. Cart Table


Column Name	Type	Description
id	INTEGER (PK)	Cart ID
user_id	INTEGER (FK)	User ID

🧾 5. Cart Items Table


Column Name	Type	Description
id	INTEGER (PK)	Cart item ID
cart_id	INTEGER (FK)	Cart ID
menu_id	INTEGER (FK)	Menu item ID
quantity	INTEGER	Quantity

📦 6. Orders Table


Column Name	Type	Description
id	INTEGER (PK)	Order ID
user_id	INTEGER (FK)	User
total_price	REAL	Total amount
status	TEXT	pending / preparing / ready
created_at	TEXT	Date & time

📋 7. Order Items Table

Column Name	Type	Description
id	INTEGER (PK)	Order item ID
order_id	INTEGER (FK)	Order ID
menu_id	INTEGER (FK)	Menu item
quantity	INTEGER	Quantity
price	REAL	Price at time